先更正一个关键点：你这份 D:\games\CCC汉化版（240428更新） 不是 PS3 版，而是 PSP 版 Fate/Extra CCC，因为包里带的是 PPSSPP，游戏本体也是单个 ISO。FateExtraCCC（汉化完成版-月研4）.iso ppsspp 1.17.1_win

我实际扫下来，内部结构大致是这样：

ISO 是标准 PSP 结构：PSP_GAME/SYSDIR、USRDIR、INSDIR
真正的大部分游戏资源都塞在一个 1.03GB 的单文件里：PSP_GAME/INSDIR/GAME.DNS
GAME.DNS 里能扫出 CpkHeader、CPKMC1.36.00、CRI、UTF、RIFF/WAVE、AT3 等标记，所以它基本可以判定是 CRI 的 CPK 资源包
另外 USRDIR/MOVIE 里有两个 .PMF 影片文件
模拟器目录里还有一个已经做好的高清纹理包 textures.zip，里面是大量 .ktx2 纹理，很多立绘/UI 图可能已经在这里被“模拟器侧重建”过了
GAME.DNS
textures.zip
怎么拆立绘、语音、文本

先拆 GAME.DNS
最优先，因为文本、原始语音、原始图片大概率都在这里。
推荐工具路线：
QuickBMS + cpk.bms
CriPakTools
CPK Browser
VGMToolbox 作为补充
目标是先把 GAME.DNS 解成完整文件树，看看里面是否出现这些典型资源：

图像：.gim、.tm2、.tex、.png
语音：.at3、.adx、.hca、.wav
文本/脚本：.msg、.bin、.dat、.scp、.mrb、.utf
立绘优先看两条线
原始游戏资源线：从 GAME.DNS 解包后找角色图、表情差分、UI 图
模拟器纹理线：直接看 textures.zip 里的 .ktx2
如果你的目标是“拿到能直接看的高清立绘”，反而建议先走 textures.zip。因为它很可能已经是替换好的高分辨率贴图，不需要先理解 PSP 原始图像格式。
如果你的目标是“完整、原始、可重组的立绘素材”，还是得回到 GAME.DNS 里找真正的底层图。

语音通常是最好拆的一类
你这个包里已经明确扫到了大量 RIFF/WAVE 和 AT3 痕迹，所以语音大概率能拆出来。
常见处理方式：
先从 CPK 解包出音频文件
如果是 .at3/ATRAC3，用 vgmstream、foobar2000 + vgmstream 或 ffmpeg 相关方案转成 wav
如果是 CRI 系的 .adx/.hca，也有成熟工具链可转
换句话说，语音的成功率通常最高。

文本是最可能需要额外分析的一类
文本未必是纯 txt，它更可能是：
剧本二进制
带控制符的消息表
按章节/场景拆分的 .msg/.bin/.dat
在汉化版里被重新打包过
你应该这样做：

先解 CPK
再全目录检索日文/中文可见字符串
如果能直接搜到中文台词，说明文本是“明文或轻编码”
如果搜不到，通常是：
自定义编码表
指针表 + 文本块
压缩脚本格式
因为这是汉化版，文本很可能已经被改写过，反而更有机会找到中文可读数据。

按难度排序
从最容易到最难，大致是：

语音
模拟器纹理包里的立绘/UI 图
原始 CPK 里的图像资源
文本脚本重建
最推荐的实操顺序
如果你想少走弯路，我建议：

先备份 ISO
解 GAME.DNS
优先确认语音格式
再确认图像格式
最后做文本定位
这样你很快就能知道“这包能不能拆”“哪类资源最值先做”。

我对这作的具体判断
这份包的资源结构不算特别恶劣，因为已经明确落在 CPK 这条成熟路线里，不是那种完全无头绪的私有封包。
所以结论是：

立绘：能拆，先看 textures.zip，再看 GAME.DNS
语音：大概率最好拆
文本：能定位，但是否容易导出重组，要看解包后脚本格式
如果你愿意，我下一步可以继续帮你做两件更具体的事之一：

给你列一份“Windows 上实际要用的工具清单 + 操作顺序”
直接帮你设计一套“先拆 GAME.DNS，再筛出立绘/语音/文本”的工作流，包括每一步怎么判断有没有拆对